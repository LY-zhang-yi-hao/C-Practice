#include <iostream>
#include <string>
#include <cctype>

using namespace std;

/*
把 C 风格字符串转换为 int 整数。
把 C 风格字符串转换为 long 整数。
把 C 风格字符串转换为 longlong 整数。
把 C 风格字符串转换为 double 浮点数。
a）如果字符串中间有非法字符，后面的内容将丢弃；
b）如果字符串的第一个字符非法或字符串为空，将返回 0。
*/

// 把 C 风格字符串转换为 int 整数。
// 逻辑：⁉️ 利用ASCII码，例如：int类型 为 '10' - '0' = 10。
int main(){
    char* str;
    cout << "input a string:";
    cin >> str;
    

    
}